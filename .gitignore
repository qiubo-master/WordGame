# 依赖
node_modules/
server/node_modules/

# 构建产物（由 CI / CloudBase 构建，不入库）
dist/

# 运行时数据：含 JWT 密钥(.secret)与本地测试库(wordquest.db)，严禁入库
server/data/

# WorkBuddy 内部状态（工具数据，非项目源码）
.workbuddy/

# 环境变量 / 密钥
.env
.env.local
.env.*.local

# 日志
*.log
npm-debug.log*

# 测试 / 构建临时产物
.smoke-*
vite.config.ts.timestamp-*.mjs


# 系统文件
.DS_Store
Thumbs.db
