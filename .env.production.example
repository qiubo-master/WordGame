# 构建「公网版」前端时，把本文件复制为 .env.production 并填入你的云函数 HTTP 域名。
# 注意：Vite 在 build 时把 VITE_ 前缀的变量内联进产物，所以必须在 `npm run build` 之前设置好。
#
# 格式：https://<你的环境ID>.api.tcloudbase.com/wqapi
# 其中 wqapi 是 cloudbase.json 里定义的函数名，部署后在控制台「HTTP 触发」处可看到完整域名。
VITE_API_BASE=https://YOUR_ENV_ID.api.tcloudbase.com/wqapi
